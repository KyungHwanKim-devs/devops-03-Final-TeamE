apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-deploy
  namespace: front
spec:
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 2
  minReadySeconds: 5
  revisionHistoryLimit: 5
  replicas: 2
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      name: front
      labels:
        app: front
    spec:
      #이미지 풀 시크릿
      imagePullSecrets:
        - name: regcred
      containers:
        - name: front
          image: 804394216826.dkr.ecr.ap-northeast-2.amazonaws.com/teame-front-dev:7b1a551b8400caf74d7d44f3ae8b4fc3114d37be
          ports:
            - containerPort: 3000
          resources:
            limits:
              memory: "256Mi"
              cpu: "1024m"
